alert ("Enter two numbers to see if the first is greater than the second");

let message = prompt('Enter the first number');
let newMessage = prompt('Enter the second number');
result = confirm( message == newMessage);